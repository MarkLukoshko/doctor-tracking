import { firstNotNone } from 'doctrack/helpers/first-not-none';
import { module, test } from 'qunit';

module('Unit | Helper | first not none');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = firstNotNone([42]);
  assert.ok(result);
});
