import { timerDisplay } from 'doctrack/helpers/timer-display';
import { module, test } from 'qunit';

module('Unit | Helper | timer display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timerDisplay([42]);
  assert.ok(result);
});
